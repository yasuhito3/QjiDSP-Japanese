# Qji(奏在)取扱説明書

**〜 コンサートホールの響きを、あなたの部屋へ 〜**

---

## 目次

1. [Qjiとは](#1-qjiとは)
2. [動作環境・必要なもの](#2-動作環境必要なもの)
3. [セットアップ](#3-セットアップ)
4. [起動方法](#4-起動方法)
5. [メインメニューの見かた](#5-メインメニューの見かた)
6. [再生中のキー操作](#6-再生中のキー操作)
7. [音場プリセット(音響フィルター)](#7-音場プリセット音響フィルター)
8. [ゲインプリセット(入力段階の音量調整)](#8-ゲインプリセット入力段階の音量調整)
9. [お気に入り機能](#9-お気に入り機能)
10. [Sonia Intelligence(SI)— AIによる音響フィードバック](#10-sonia-intelligencesi--aiによる音響フィードバック)
11. [音声コマンド](#11-音声コマンド)
12. [ラジオ機能](#12-ラジオ機能)
13. [ストリーミング再生(Qobuz / SoundCloud / YouTube Music)](#13-ストリーミング再生qobuz--soundcloud--youtube-music)
14. [AirPlay / DLNA レシーバー機能](#14-airplay--dlna-レシーバー機能)
15. [Now Playingミラー(スマホ・タブレットでの表示)](#15-now-playingミラースマホタブレットでの表示)
16. [プリセット管理(設定の保存)](#16-プリセット管理設定の保存)
17. [トラブルシューティング](#17-トラブルシューティング)
18. [用語集](#18-用語集)

---

## 1. Qjiとは

**Qji(奏在)** は、Linux上で動作する高音質音楽再生システムです。単なる音楽プレイヤーではなく、

- **CamillaDSP・ffmpeg・ALSA** を組み合わせた本格的なオーディオパイプライン
- **Musikverein(ウィーン楽友協会)ホール・アコースティック** を模したリアルタイム音響処理
- 曲のジャンル・ムード・テンポを自動解析して選曲できるデータベース機能
- 好きな瞬間に好きな曲をすぐ呼び出せる**お気に入り機能**
- 音の感想を日本語で伝えるだけで音響を微調整してくれる**Sonia Intelligence(SI)**
- ラジオ・Qobuz・SoundCloud・YouTube Musicなどのストリーミング再生
- AirPlay・DLNA(UPnP)受信によるワイヤレス再生
- スマートフォンのブラウザでジャケットと再生状況を確認できる「Now Playingミラー」

これらを一つに束ねた、**「聴く」体験そのものを設計するための音楽再生システム**です。

開発者自身が実際に自宅のオーディオ環境で日々使いながら、気づいたことを一つ一つ機能として積み重ねてきました。効率よりも「良い音で、良い曲に出会える」ことを優先して作られています。

---

## 2. 動作環境・必要なもの

### 必須

| 項目 | 内容 |
|---|---|
| OS | Linux(Ubuntu / Xubuntu 系で動作確認) |
| Python | Python 3(`mutagen` パッケージが必要) |
| オーディオ | ffmpeg、ALSA(`aplay`) |
| 画像表示 | `feh`(ジャケット画像表示)、ImageMagick(`convert`、曲情報オーバーレイ用) |
| 音楽ライブラリ | あらかじめメタデータ解析済みのデータベースファイル(`~/music_mood_db.json`) |

このデータベースは、付属の解析スクリプト(例:`music_analyzer2.py` 系)で音楽フォルダをスキャンして作成します。Qji自体はこのJSONデータベースを読み込んで動作するため、**先に音楽ライブラリの解析を済ませておく必要があります**。

### 任意(あると使える機能が増えます)

| 機能 | 必要なもの |
|---|---|
| 音声コマンド操作 | `vosk`、`sounddevice`(日本語音声認識モデルを`~/vosk-model-ja-0.22`に配置) |
| Sonia Intelligence(SI) | 同梱の `sonia_intelligence` モジュール一式 |
| AirPlay受信 | `shairport-sync`、`avahi-daemon`、ALSAループバック(`snd-aloop`) |
| DLNA/UPnP受信 | `gmediarender`、ALSAループバック(`snd-aloop`) |
| Qobuz / SoundCloud / YouTube Music再生 | 各対応モジュール(`qji_qobuzdsp.py` 等)、`yt-dlp` |
| Now Playingミラー | 同一Wi-Fi内のスマートフォン等のブラウザ |

すべての機能を最初から揃える必要はありません。ローカルの音楽ライブラリ再生だけでも、Qjiの音響エンジンは十分に楽しめます。

---

## 3. セットアップ

1. 必要なパッケージをインストールします(ffmpeg、ALSA関連ツール、feh、ImageMagickなど、お使いのディストリビューションのパッケージマネージャで導入してください)。
2. Python側の依存パッケージを導入します(`mutagen` は必須、`vosk`/`sounddevice` は音声コマンドを使う場合のみ)。
3. 音楽ファイルを、`qji.py` 内で設定されている音楽フォルダ(例:`~/Music`、外付けドライブのマウント先など)に配置します。対応形式は `.wav / .flac / .wma / .aiff / .aif / .mp3 / .m4a / .aac / .ogg` です。
4. 付属の解析スクリプトを実行し、`~/music_mood_db.json` を生成します。これがQjiの選曲データベースになります。
5. AirPlay / DLNA を使う場合は、`snd-aloop` カーネルモジュールを読み込んでおきます。

   ```
   sudo modprobe snd-aloop
   ```

   永続化するには `/etc/modules` に `snd-aloop` を追記してください。

6. 音声コマンドを使う場合は、日本語Voskモデルを `~/vosk-model-ja-0.22` に展開してください。

---

## 4. 起動方法

### 4.1 デスクトップアイコンのダブルクリックで起動する

インストール時に、デスクトップアイコンが自動的に作成されます。そのアイコンをダブルクリックするだけでQjiを起動できます(初回起動時から利用できます)。

### 4.2 ターミナルから起動する(alternative)

ターミナルから次のように実行することもできます。

```
python3 qji.py
```

起動すると、まずスプラッシュ画面(「奏在」のロゴ)が数秒間表示され、その後データベースが読み込まれてメインメニューが表示されます。

音声認識を無効にして起動したい場合は、`--no-voice` オプションを付けて起動してください。

---

## 5. メインメニューの見かた

メインメニューは大きく分けて「**選曲モード**」「**音響・出力設定**」「**外部機能**」の3グループから構成されています。

### 選曲モード

| キー | 内容 |
|---|---|
| `0` | 全曲からランダム再生(最大500曲) |
| `1` | テンポ設定(BPM)で選曲 |
| `2` | 作曲家名で選曲 |
| `3` | 演奏者名で選曲 |
| `4` | 指揮者名で選曲 |
| `5` | ジャンルで選曲 |
| `6` | ムードで選曲 |
| `7` | 🔍 キーワード検索(タイトル・作曲家・演奏者などを横断検索) |
| `8` | 🔷 複数条件(作曲家+ジャンル等)を組み合わせて選曲 |
| `9` | 📊 ライブラリ全体のムード統計を表示 |
| `J` | 🖼️ ジャケット画像を一覧表示して視覚的に選曲 |
| `N` | 🆕 最近ライブラリに追加した音源を、ジャケット表示で選曲 |

### 音響・出力設定

| キー | 内容 |
|---|---|
| `A` | 音響プリセット(ボーカルを前に/ソリストを前に等、簡易EQ) |
| `G` | ゲインプリセット(後述、入力段階の音量調整) |
| `L` | 音量一定化(ラウドネスノーマライゼーション)のON/OFF |
| `T` | 耳鳴り低減モード(高音域をやや抑える)のON/OFF |
| `W` | 音場調整(Air Particle Layer / ピンクノイズ空気層)のON/OFF |
| `V` | 楽友協会ルームエフェクト(Musikverein反響)のON/OFF |
| `F` | 音響プリセット(ジャンル別フィルター、後述の11種類)の切り替え |
| `E` | エコーモード(クラシック向け/ジャズボーカル向け)の切り替え |
| `Z` | ギャップレス再生のON/OFF(フォルダーモード時に有効) |
| `U` | アップサンプリングのON/OFF・目標サンプルレート設定 |

### 外部機能・管理

| キー | 内容 |
|---|---|
| `R` | 📻 ラジオステーション再生 |
| `P` | 💾 プリセット管理(現在の音響設定一式を名前付きで保存・復元) |
| `K` | ⭐ お気に入り管理 |
| `M` | 📱 Now Playingミラー(スマホ等のブラウザ表示)のON/OFF |
| `QB` | 🎵 Qobuz ストリーミング再生 |
| `S` | 🟠 SoundCloud ストリーミング再生 |
| `Y` | 🔴 YouTube Music ストリーミング再生 |
| `AP` | 📡 AirPlay レシーバー(iPhone/Macから受信) |
| `DL` | 📻 UPnP/DLNA レシーバー(BubbleUPnP等から受信) |
| `X` | 🔄 DSP出力デバイスの再認識(後から接続したDACを掴み直す) |
| `Q` | 終了 |

---

## 6. 再生中のキー操作

曲の再生中は、ターミナル上でキーを押すことでリアルタイムに操作できます(Enterキーは不要、押した瞬間に反映されます)。

| キー | 動作 |
|---|---|
| `r` | 曲を最初から再生し直す |
| `f` | 同じフォルダー内の曲を順番に再生するモードへ切り替え |
| `n` | 次の曲へ |
| `b` | 前の曲へ |
| `i` | ジャケット画像を再表示 |
| `o` | ⭐ 今流れている曲をお気に入りに登録 |
| `w` | 音場調整(Air Particle Layer)をON/OFF |
| `c` | フィルタープリセット(音場プリセット)をその場で変更 |
| `g` | ゲインプリセットを順番に切り替え |
| `+` / `=` | 出力ゲインを+1dB |
| `-` | 出力ゲインを-1dB |
| `s` | 現在の全音響設定をプロファイルとして保存(曲やアルバム単位で記憶) |
| `q` | 再生を終了してメインメニューに戻る |

**Sonia Intelligenceが利用可能な場合のみ追加されるキー:**

| キー | 動作 |
|---|---|
| `z` | 音の感想を日本語で入力(「ピアノをもっと前に」など) |
| `h` | ホール(音響空間)を選択 |
| `p` | SIプロファイルを選択 |
| `x` | SI音響プリセットを番号で選択 |
| `a` | 現在のアルバムに音響プリセットを手動登録 |

再生中に画面下部へ操作ガイドが常時表示されるので、覚えていなくても迷いません。

---

## 7. 音場プリセット(音響フィルター)

`F` キー(メインメニュー)または `c` キー(再生中)で切り替えられる、ジャンル・シーンに応じた音響フィルタープリセットです。全11種類。

| プリセット | 特徴 |
|---|---|
| 🎻 Musikverein (Orchestra) | ウィーン楽友協会ホールの残響を模したオーケストラ向けデフォルト設定 |
| 🎹 Piano | ピアノ独奏・協奏曲向けに調整 |
| 🏠 Chamber | 室内楽向け、自然な近接感を重視 |
| 🎙 Vocal | 歌曲・声楽向け、声の質感を前に出す |
| 🎷 Jazz | ジャズ・ポップス向け |
| 🌿 Calm(安らぎ) | 静水面のような穏やかな響き。リラックスして聴きたいときに |
| 🌊 Deep(深淵) | より深く沈み込むような音場。夜間の集中リスニング向け |
| 🌐 Spatial(3D空間音響) | ヘッドホン向けの立体音響処理 |
| 📻 Radio(標準) | ラジオストリーム向けの標準的な処理 |
| ⚪ Bypass(音響処理なし) | 音響処理を一切かけない、リファレンス用 |
| 💿 Vinyl Emotion(アナログ質感) | アナログレコードのような質感を演出 |

再生中に「あれ、この曲には合わないな」と感じたら `c` キーでその場で切り替えられます。ジャケット画像の左上バッジにも現在のプリセット名が表示されるので、切り替え忘れにもすぐ気づけます。

---

## 8. ゲインプリセット(入力段階の音量調整)

`G` キー(メインメニュー)または `g` キー(再生中、順送り切り替え)で設定します。

| プリセット | 値 | 用途 |
|---|---|---|
| クラシック用 | 0dB | ヴァイオリン等の繊細な音を歪ませたくない場合 |
| 汎用 | -1.5dB | バランス型、様々なジャンルに対応 |
| ジャズ・ポップス用 | -3.5dB | 音圧の大きい録音での歪みを防止 |
| ラウド素材用 | -5dB | 大音量録音・ライブ音源など |

ジャンルに応じて自動的にジャズ・ポップス用へ連動する機能もありますが、`g` キーで手動選択すると自動連動は解除され、選んだ設定が維持されます。

---

## 9. お気に入り機能

再生中にふと「これは記録しておきたい」と思った瞬間、**`o` キーひとつ**でその曲をお気に入りに登録できます。入力待ちで再生が止まることはありません。

登録時には、その曲のジャンル・ムード情報が自動的に**タグ**として付与されます。あとから `K` メニューで自由に整理できます。

### お気に入り管理メニュー(`K`)

| 番号 | 内容 |
|---|---|
| 1 | お気に入り一覧を表示(タグ・メモ・ファイルの存在確認付き) |
| 2 | タグで絞り込んで一覧表示(番号選択、または名前で入力可・大文字小文字は区別しません) |
| 3 | お気に入り全曲をシャッフル再生 |
| 4 | タグを指定して、そのタグの曲だけ再生 |
| 5 | 一覧から1曲だけ選んで再生 |
| 6 | 既存のお気に入りのタグ・メモを編集 |
| 7 | お気に入りから削除 |
| 8 | ファイルの場所が変わった・消えたお気に入りをまとめて検出し整理 |

### 使い方の一例

1. 再生中、心に残った曲で `o` を押す(自動でジャンル/ムードタグが付く)
2. あとで時間のあるときに `K` → `6` で、「シューベルト」「歌曲」「小品」のような自分の言葉のタグやメモ(「昔スコアを買って歌った」等)を追加
3. 次からは `K` → `4` でそのタグを選ぶだけで、似た系統の曲だけをまとめて呼び出せる

登録済みお気に入りが増えてきても、タグによる絞り込みと番号選択のおかげで迷わず管理できます。

---

## 10. Sonia Intelligence(SI)— AIによる音響フィードバック

Sonia Intelligence(SI)は、日本語の自然な感想を音響調整に変換するシステムです。`sonia_intelligence` モジュールが読み込まれている場合のみ有効になります。

| キー | 機能 |
|---|---|
| `z` | 感想を自由入力。例:「ピアノをもっと前に」「ホールの奥行きをもっと」「低音が重すぎる」「弦の艶をかなり強調」 |
| `h` | ホール(音響空間)を選択 |
| `p` | 保存済みのSIプロファイルを選択して適用 |
| `x` | SI音響プリセットを番号で選択 |
| `a` | 今のアルバムに対して音響プリセットを手動で紐付け登録 |

`z` で入力した内容は、そのまま音響パラメータへ反映され、簡単な説明とともに画面に表示されます。「なんとなく違う」と感じたときに、EQのdB値などを意識せず、言葉で微調整できるのが特徴です。

---

## 11. 音声コマンド

Voskによるオフライン音声認識で、マイクに向かって話しかけるだけで操作できます(`VOICE_RECOGNITION_AVAILABLE` な環境かつ有効時)。

| 発話例 | 動作 |
|---|---|
| 「フォルダ」「フォルダー」 | フォルダー順次再生モードへ |
| 「テンポ」 | テンポモードへ |
| 「作曲家」 | 作曲家モードへ |
| 「演奏」「オーケストラ」 | 演奏者モードへ |
| 「指揮」 | 指揮者モードへ |
| 「ジャンル」 | ジャンルモードへ |
| 「ムード」 | ムードモードへ |
| 「次」 | 次の曲へ |
| 「前」 | 前の曲へ |
| 「もう一度」「最初から」「リプレイ」 | 曲を最初から再生 |
| 「終了」「ストップ」「停止」 | 再生を終了 |
| 「画像」「ジャケット」 | ジャケット画像を再表示 |

インターネット接続不要のオフライン認識のため、プライバシー面でも安心です。

---

## 12. ラジオ機能

`R` キーで、あらかじめ登録されたインターネットラジオ局の一覧(国旗アイコン付き)が表示されます。番号を選ぶだけで再生が始まり、通常のローカル再生と同じ音響フィルターチェーンが適用されます。

再生を終えてもラジオメニューに留まるので、次々と局を試聴できます。`0` でメインメニューに戻ります。

---

## 13. ストリーミング再生(Qobuz / SoundCloud / YouTube Music)

| キー | サービス |
|---|---|
| `QB` | Qobuz(ハイレゾ対応ストリーミング) |
| `S` | SoundCloud |
| `Y` | YouTube Music |

いずれも、現在設定中のゲインプリセット・耳鳴り低減・楽友協会ルームエフェクト・Air Particle Layer・エコーモード・出力デバイスといった音響設定を**そのまま引き継いで**再生します。ローカルファイルと同じ「Qjiの音」で、ストリーミング音源も楽しめます。

---

## 14. AirPlay / DLNA レシーバー機能

### AirPlay(`AP`)

iPhoneやMacから、AirPlay経由でQjiに音声を送り、Qjiの音響フィルターチェーンを通して再生できます。`shairport-sync` と `avahi-daemon`、ALSAループバックが必要です。

### DLNA/UPnP(`DL`)

BubbleUPnP等のDLNAコントローラーから、Qjiをレンダラーとして選択して再生できます。`gmediarender` とALSAループバックが必要です。

どちらの受信モードでも、再生中は次のキーが使えます。

| キー | 動作 |
|---|---|
| `c` | フィルター変更 |
| `h` | 楽友協会ルームエフェクトのON/OFF |
| `a` | Air Particle LayerのON/OFF |
| `x` | SIプリセット選択 |
| `+` / `-` | 音量調整 |
| `q` | 停止 |

必要なコンポーネントが見つからない場合は、インストール方法やコマンドが画面に案内されます。

---

## 15. Now Playingミラー(スマホ・タブレットでの表示)

`M` キーでミラーサーバーを起動すると、同一Wi-Fi内のスマートフォンやタブレットのブラウザから、`http://(PCのIPアドレス):(ポート番号)/` へアクセスするだけで、再生中のジャケット画像・曲情報・現在の音場プリセットをリアルタイムに確認できます。

PC本体側のジャケット表示にも、同じ音場プリセット情報(フィルター名+ゲインプリセット名)がバッジとして重ねて表示されるため、「設定がずれている」ことに再生中すぐ気づけます。

---

## 16. プリセット管理(設定の保存)

`P` キーから、現在の音量・出力デバイス・音響プリセット・ゲインプリセット・音量一定化・耳鳴り低減・楽友協会ルームエフェクト・ギャップレス・アップサンプリング・フィルタープリセットなど、**音響設定一式**をまとめて名前付きで保存できます。

| 番号 | 内容 |
|---|---|
| 1 | 現在の設定をプリセットとして保存 |
| 2 | プリセットを読み込んで適用(番号または名前で選択) |
| 3 | プリセット一覧を表示 |
| 4 | プリセットを削除 |
| 5 | 現在の設定を表示 |

聴取シーン(「夜の一人聴き用」「大音量パーティー用」など)ごとに設定を作っておくと、毎回の微調整が不要になります。

---

## 17. トラブルシューティング

| 症状 | 確認事項 |
|---|---|
| 起動時に「データベースファイルが見つかりません」と出る | 先に解析スクリプト(`music_analyzer2.py` 等)でライブラリを解析し、`~/music_mood_db.json` を生成してください |
| 音声コマンドが反応しない | `vosk`/`sounddevice` がインストールされているか、`~/vosk-model-ja-0.22` にモデルが展開されているか、USBマイクが認識されているかを確認してください |
| AirPlay/DLNAが使えない | `snd-aloop` が読み込まれているか(`sudo modprobe snd-aloop`)、`shairport-sync`/`gmediarender` がインストールされているかを確認してください |
| Sonia Intelligenceのキー(`z`/`h`/`p`/`x`/`a`)が出てこない | `sonia_intelligence` モジュールの読み込みに失敗している可能性があります。起動時のメッセージを確認してください |
| ジャケット画像に曲情報が重ならない | ImageMagick(`convert`コマンド)がインストールされているか確認してください |
| 出力先DACが認識されない | メインメニューの `X`(DSP出力デバイスの再認識)を試してください |

---

## 18. 用語集

| 用語 | 説明 |
|---|---|
| 奏在(そうざい) | Qjiの日本語プロジェクト名。「音楽がそこに在る」という意味を込めています |
| Musikverein(ムジークフェライン) | ウィーンにある世界的に名高いコンサートホール。その残響特性をffmpegフィルターで模しています |
| Air Particle Layer | ピンクノイズによる微弱な空気層を重ね、実際のホールで感じる「空気の存在感」を演出する機能 |
| フィルタープリセット(音場プリセット) | ジャンル・シーンに応じた音響フィルターチェーンのセット(全11種類) |
| ゲインプリセット | 入力段階での音量調整プリセット(歪み防止) |
| Sonia Intelligence(SI) | 日本語の感想入力から音響を調整するAI支援システム |
| Now Playingミラー | スマホ等のブラウザで再生状況を確認できるミラー表示機能 |

---

*本マニュアルはQji開発者本人により作成されました。ご不明な点や改善のご要望があれば、GitHubのリポジトリまでお気軽にどうぞ。*
