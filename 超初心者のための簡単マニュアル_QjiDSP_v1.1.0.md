# 超初心者のための簡単マニュアル
### ―― Linuxのインストールから QjiDSP の音出しまで ――

---

## はじめに

このマニュアルは、パソコンやLinuxをほとんど触ったことがない方でも、
**Xubuntu（ズブントゥ）というLinuxをインストールし、QjiDSP（音楽再生ソフト）で実際に音を鳴らすところまで**
到達できるように書かれています。

次のステップ（プレイリストの作り方、各種プリセットの使い方など）は本マニュアルの範囲外です。
「まず音が出る」ことをゴールにしています。

対象読者：パソコンの電源の入れ方は分かるが、Linuxもターミナル（コマンド入力画面）も初めて、という方。

---

## 用意するもの

- パソコン本体（下記「第1章」参照）
- インターネット接続（ダウンロード用）
- USBメモリ 1本（8GB以上、中身は消えるので空にしておく）
- （任意・強く推奨）USB DAC（外付けのD/Aコンバーター）とヘッドホン／スピーカー
- 手元に置くための、このマニュアルを見られるスマホやタブレットなど別の端末（作業中はPC画面がLinuxのインストール画面になるため）

---

## 第1章　PCの準備

1. **専用の1台を用意するのがおすすめです。** 既に使っているWindows/Macに上書きすると、そのPCの元のデータは消えます。中古PCや使わなくなったノートPCへの新規インストールがもっとも安全です。
2. **最低限の目安**
   - CPU：ここ10年以内のもの（Intel/AMD問わず）
   - メモリ：4GB以上（8GB以上あると快適）
   - ストレージ：空き20GB以上（SSD推奨）
   - USBポートが1つ以上空いていること（DACをUSB接続する場合）
3. PCの電源を入れ、**BIOS/UEFIの起動順序でUSBから起動できるか**だけ確認できると安心です（分からなければ第4章で実際に試しながらで大丈夫です）。

---

## 第2章　Xubuntuのダウンロード

1. 別のPCやスマホのブラウザで、Xubuntu公式サイトにアクセスします。
   - `https://xubuntu.org/download/`
2. **「24.04 LTS」など「LTS」と書かれたバージョン**を選びます（LTS＝長期サポート版で、初心者にはこちらが安全です）。
3. 「64-bit」のISOファイル（`.iso`という拡張子のファイル、3GB前後）をダウンロードします。
4. ダウンロードが終わったら、ファイルがどこに保存されたか（通常は「ダウンロード」フォルダ）を確認しておきます。

---

## 第3章　インストール用USBメモリの作成

ダウンロードしたISOファイルを、USBメモリに書き込んで「起動できるUSBメモリ」に変えます。

1. USBメモリ書き込みソフト **「balenaEtcher」**（無料）を、作業しているPC（Windows/Mac）にダウンロードしてインストールします。
   - `https://etcher.balena.io/`
2. USBメモリをPCに挿します（中のデータは消えるので、必要なら事前にバックアップ）。
3. balenaEtcherを起動し、
   - 「Flash from file」→ ダウンロードしたXubuntuのISOファイルを選択
   - 「Select target」→ 挿したUSBメモリを選択
   - 「Flash!」をクリック
4. 書き込みが終わる（数分〜十数分）まで待ちます。終わったらUSBメモリを取り外します。

---

## 第4章　Xubuntuのインストール

1. 作成したUSBメモリを、第1章で用意したインストール先のPCに挿します。
2. PCの電源を入れ、起動直後に **F2 / F10 / F12 / Delete / Esc** のいずれか（メーカーにより異なる）を連打し、「起動デバイス選択画面（Boot Menu）」を開きます。
3. USBメモリを選んで起動すると、Xubuntuのメニュー画面が出ます。
   - 「Try Xubuntu（試用）」→ まず試してから
   - 「Install Xubuntu（インストール）」→ そのままインストール
   のどちらでも構いません。
4. 画面の指示に従います。主な選択項目：
   - 言語：日本語
   - キーボードレイアウト：日本語
   - 「アップデートとサードパーティ製ソフトウェア」→ **両方ともチェックを入れる**（あとで音楽ソフトを動かすために必要なドライバ類が入りやすくなります）
   - インストールの種類：**「ディスクを削除してXubuntuをインストール」**（そのPCをまるごとXubuntu専用にする場合）
     - ※他のOSと共存させたい場合は「それ以外（手動）」を選びますが、初心者には非推奨です。
   - タイムゾーン：Tokyo
   - ユーザー名・パスワードを設定（**パスワードは忘れないようにメモしておく**）
5. インストールが終わったら再起動を促されるので、USBメモリを抜いて再起動します。
6. デスクトップ画面が表示されたら、Xubuntuのインストール完了です。

---

## 第5章　インストール後の初期設定

デスクトップ画面が出たら、以下を行います。

1. **アップデート**
   - 画面左上のアプリケーションメニュー → 「設定」→「ソフトウェアの更新」を開き、更新があれば全て適用し、再起動します。
2. **ターミナルを開く**
   - キーボードで `Ctrl` + `Alt` + `T` を押すと、黒い画面（ターミナル）が開きます。ここから先はこの画面にコマンドを入力していきます。
   - コマンドは1行ずつ入力し、最後に `Enter` キーを押して実行します。
3. **システムを最新化し、必須ツールを入れる**（ターミナルに以下を1行ずつ入力）
   ```bash
   sudo apt update
   sudo apt upgrade -y
   sudo apt install -y python3 python3-pip python3-venv alsa-utils
   ```
   - `sudo` の後にパスワードを聞かれたら、第4章で設定したパスワードを入力します（画面には何も表示されませんが入力はされています）。

---

## 第6章　ウェブブラウザーの準備

Xubuntuには標準で「Firefox」というブラウザが入っています。デスクトップ上、またはアプリケーションメニューにアイコンがあります。

- そのままFirefoxを使う場合は特に作業不要です。
- Google Chromeを使いたい場合は、Firefoxで `https://www.google.com/chrome/` にアクセスし、Linux版（.debファイル）をダウンロードしてダブルクリックでインストールします。

以降の「GitHubからダウンロード」は、このブラウザを使います（ターミナルは使いません）。

---

## 第7章　GitHubからQji関連ソフトをダウンロード

ブラウザで、3つのリポジトリのページをそれぞれ開き、ZIPファイルとしてダウンロードします。

1. ブラウザで以下の3つのページに、順番にアクセスします。
   - Qji本体：`https://github.com/yasuhito3/Qji-Network-Audio-Player`
   - QjiDSP：`https://github.com/yasuhito3/QjiDSP-Japanese`
   - ピークモニター：`https://github.com/yasuhito3/Qji-peak-monitor`
2. 各ページで、右上のあたりにある緑色の **「Code」** ボタンをクリックし、出てきたメニューの一番下にある **「Download ZIP」** をクリックします。
3. 3つとも同様に行うと、通常「ダウンロード（Downloads）」フォルダに、次の3つのZIPファイルが保存されます。
   - `Qji-Network-Audio-Player-main.zip`
   - `QjiDSP-Japanese-main.zip`
   - `Qji-peak-monitor-main.zip`
4. ファイルマネージャーで「ダウンロード」フォルダを開き、3つのZIPファイルをそれぞれ**右クリック→「ここに展開」**します。すると、次の3つのフォルダができます。
   - `Qji-Network-Audio-Player-main`
   - `QjiDSP-Japanese-main`
   - `Qji-peak-monitor-main`

---

## 第8章　各インストーラーを実行する

3つのフォルダそれぞれに、ダブルクリックで実行できるインストーラー（`.desktop`ファイル）が入っています。3つとも、基本の操作は同じです。

**基本の操作（3つとも共通）**
- 対象の `.desktop` ファイルをダブルクリックします。
- 初回は「信頼されていないアプリケーションランチャー」といった警告が出ることがあります。その場合は、
  1. 出てきたダイアログに「起動」や「信頼して起動」のようなボタンがあれば、それをクリック。
  2. ボタンが出ない・警告が消えない場合は、そのファイルを**右クリック→「プロパティ」→「アクセス権」タブ**を開き、「プログラムとして実行を許可する」にチェックを入れてから、もう一度ダブルクリックします。
- あとは画面に出る指示に従って進めます。

**1. Qji本体のインストール**
   - `Qji-Network-Audio-Player-main` フォルダ → その中の `qji_installer` フォルダを開く
   - 中にある **「インストール.desktop」** をダブルクリックして、上記の基本操作でインストールを進めます。

**2. QjiDSPのインストール**
   - `QjiDSP-Japanese-main` フォルダ → その中の `qjidsp_installer` フォルダを開く
   - 中にある **「QjiDSPインストール.desktop」** をダブルクリックして、同様にインストールを進めます。
   - このインストール中に、CamillaDSPやffmpegなど、音を出すために必要なソフトが自動的に導入されます（少し時間がかかることがあります）。

**3. ピークモニターのインストール**
   - `Qji-peak-monitor-main` フォルダを直接開く（サブフォルダはありません）
   - 中にある **「INSTALL.desktop」** をダブルクリックして、同様にインストールを進めます。

うまく起動しない・エラーが出る場合は、そのエラーメッセージをメモしておいてください（末尾「おわりに・困ったときは」参照）。

---

## 第9章　音源（DAC）の準備と接続

1. USB DACをお持ちの場合は、この段階でPCのUSBポートに接続します。
2. ターミナルで、Linuxが認識しているか確認します：
   ```bash
   aplay -l
   ```
   一覧に、お使いのDACの名前（例：`Amanero`など）が表示されていれば認識成功です。
3. DACにヘッドホン、またはアンプ経由でスピーカーを接続しておきます。
4. DACを使わず、PC本体のイヤホン端子から音を出したい場合は、この章の作業は不要です。

---

## 第10章　音楽ライブラリーの構築

Xubuntuには最初から「音楽」（Music）という名前のフォルダが用意されていることがありますが、日本語表示だと「音楽」という名前になっている場合があり、その場合Qjiのライブラリー構築スクリプトが正しく認識しないことがあります。そのため、**英語名「Music」というフォルダを新しく作る**ことをおすすめします。

1. デスクトップ左のファイルマネージャーを開き、ホームフォルダ（家のアイコン）を表示します。
2. 何もない場所で**右クリック**し、メニューから **「フォルダーの作成」**（または「新しいフォルダー」）を選びます。
3. フォルダ名を、**半角英字で `Music`** と入力します（日本語の「音楽」ではなく、必ずアルファベットの `Music` にしてください）。
4. 手持ちの音楽ファイル（mp3、flacなど）を、USBメモリや外付けHDD経由で、この新しく作った `Music` フォルダにドラッグ＆ドロップでコピーします。
5. 最低でも1曲、`Music` フォルダに入っていることを確認します。まずは1曲で音出しを試し、あとから曲を増やしていけば十分です。
6. デスクトップにある **「音楽ライブラリー構築」** アイコンをダブルクリックします。処理が始まり、`Music` フォルダの中の曲がライブラリーとして読み込まれます。完了するまで少し待ちます。

---

## 第11章　Qjiを起動していよいよ音出し

1. 第8章のインストールが終わると、デスクトップに **「Qji奏在」** というアイコンが作成されています。
2. そのアイコンをダブルクリックすると、Qji本体が起動します。
3. 起動すると、ターミナル画面に **「出力サウンドカードを選択してください」** といった表示が出ます。第9章で確認したDACの番号を選んで入力します。
4. 続いて、**音声認識（マイク）の設定**として「1」か「2」を選ぶよう表示されるので、どちらかを選択します。
5. これで音楽再生のための初期画面（メインメニュー）が表示されます。
6. まずは **「0」番を入力**すると、`Music` フォルダの曲がランダムに再生されます。
7. 音が出ればゴールです。もし音が出ない場合は、
   - 第9章の `aplay -l` でDACが認識されているか再確認
   - Xubuntuの右上のスピーカーアイコンで音量がミュートになっていないか確認
   - 一旦ヘッドホン/スピーカーの接続を確認
   してから、もう一度実行してみてください。

---

## おわりに・困ったときは

- コマンドを打ってエラーが出た場合は、エラーメッセージをそのままメモ（またはスクリーンショット）しておくと、あとで調べたり質問したりするときに役立ちます。
- 各リポジトリのREADME.mdには、より詳しい・最新のインストール手順が書かれています。本マニュアルと内容が異なる場合はREADMEを優先してください。
- ここから先（プレイリスト作成、各種プリセットの使い方、DSPによる音場調整など）は、次のステップとして別途ご案内します。

お疲れ様でした。ここまでで「音が出る」状態が完成です。
